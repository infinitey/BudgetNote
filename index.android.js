import { AppRegistry　} from 'react-native';
import App from './app/App'

//Takes in 'YourProjectName' and a function that returns your root component
AppRegistry.registerComponent('BudgetNote', () => App);
